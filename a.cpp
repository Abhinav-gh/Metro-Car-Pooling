#include <bits/stdc++.h>
using namespace std;
#define int long long
#define ld long double
#define fn for(long long i=0;i<n;i++)
#define fm for(long long j=0;j<m;j++)
#define f(n) for(long long i=0;i<n;i++)
#define hashmap1 unordered_map<long long,long long>
#define hashmap2 unordered_map<string,long long>
#define pii pair<int,int>

void solve(){
    int n;
    cin>>n;
    vector<int> a(n);
    fn cin>>a[i];
    int mx=0;
    for(int i=0;i<n;i++){
        if
    }
}

int32_t main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long mod = 1e9+7;
    long long t;
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
}